import React, { FunctionComponent } from "react";

const AppContent: FunctionComponent = (): JSX.Element => {
  return <div>Earn Bills...</div>;
};

const App: FunctionComponent = (): JSX.Element => {
  return <AppContent />;
};

export default App;
